[build]
  command = "npm install && npm run build -- --configuration production"
  publish = "dist/gastos/browser"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
